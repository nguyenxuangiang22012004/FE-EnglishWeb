# Test Report - Quản lý khối lớp (Grade CRUD) - 2026-09-17

Status: fail
Tester: AI Tester (Claude) — cần người review
Environment: Local dev `next dev --turbopack` tại `http://localhost:3000`, API `https://api-enspire-develop.xlms.vn`, Chromium (Playwright 1.55), Node 20.11.0, Windows 10
Build: `develop` @ `079bbc6b`
Account: Admin `truongadmin` (password từ `TEST_ADMIN_PASSWORD` trong `.env`, không ghi vào report)
Related:
  - Test cases: docs/test-cases/schools/grade.md
  - Traceability: docs/test-cases/schools/traceability.md
  - Automation: tests/e2e/schools/grade-crud.spec.ts
  - Bug: docs/test-cases/schools/bugs/GRD-BUG-001.md → GRD-BUG-005.md
  - Requirement: docs/requirements/schools/grade.md

## Scope

Chức năng quản lý khối lớp tại `/[locale]/admin/grades` với **role Admin**: xem danh sách, tìm kiếm, phân trang, thêm, sửa, xóa, validation, lỗi API, locale `en`.

Loại task: A (feature hiện có, `docs/requirements/change-log.md` chưa có dòng cho grade) + C (viết automation).

Ngoài phạm vi (xác nhận 2026-09-17):

- Role khác Admin (teacher, student, school, chưa đăng nhập).
- Kiểm tra trùng lặp khối lớp — hệ thống chưa có verify trùng lặp.
- Trạng thái active/inactive.
- Các màn hình dùng grade làm dropdown/bộ lọc (flip book, program, export report, `academic-filters`).

Xác nhận nghiệp vụ dùng làm tiêu chí: số khối **không giới hạn 1-12**.

## Kết quả

| TC-ID | Scenario | Loại | Kết quả | Bug |
| --- | --- | --- | --- | --- |
| GRD-TC-001 | Mở trang từ sidebar, bố cục, các cột | Auto | Pass | |
| GRD-TC-002 | Danh sách khớp dữ liệu API, thông tin phân trang | Auto | Pass | |
| GRD-TC-003 | Cột Ngày tạo: ngày mặc định → "-" | Auto (mock) | **Fail** | GRD-BUG-001 |
| GRD-TC-004 | Khối lớp mới tạo hiển thị ngày hôm nay | Auto | Pass | |
| GRD-TC-005 | Tìm kiếm theo từ khóa (debounce, page=1) | Auto | Pass | |
| GRD-TC-006 | Tìm kiếm không có kết quả | Auto | Pass | |
| GRD-TC-007 | Ô chọn số dòng/trang hiển thị đúng | Auto (mock) | **Fail** | GRD-BUG-002 |
| GRD-TC-008 | Đổi số dòng/trang, chuyển trang | Auto (mock) | Pass | |
| GRD-TC-009 | Lỗi tải danh sách → toast | Auto (mock) | Pass | |
| GRD-TC-010 | Trạng thái active/inactive | — | Out-of-scope | |
| GRD-TC-011 | Tạo khối lớp hợp lệ | Auto | Pass | |
| GRD-TC-012 | Bỏ trống tất cả trường | Auto | Pass | |
| GRD-TC-013 | Số khối 0, -1, 1.5 bị chặn; nhập được 1, 12 | Auto | **Fail** (chỉ phần hiển thị lỗi) | GRD-BUG-005 |
| GRD-TC-014 | Tên chỉ khoảng trắng; trim khi lưu | Auto | Pass | |
| GRD-TC-015 | Tạo trùng bị từ chối | — | Out-of-scope | |
| GRD-TC-016 | Hủy/Esc dialog tạo | Auto | Pass | |
| GRD-TC-017 | Lỗi server khi tạo, khóa nút khi đang lưu | Auto (mock) | Pass | |
| GRD-TC-018 | Số khối > 12 được chấp nhận | Auto (cover bởi GRD-TC-011, số khối 500-899) | Pass | |
| GRD-TC-019 | Dialog sửa điền sẵn dữ liệu | Auto | Pass | |
| GRD-TC-020 | Sửa khối lớp hợp lệ | Auto | **Fail** | GRD-BUG-003 |
| GRD-TC-021 | Validation khi sửa | Auto | Pass | |
| GRD-TC-022 | Hủy sửa | Auto | Pass | |
| GRD-TC-023 | Sửa thành trùng bị từ chối | — | Out-of-scope | |
| GRD-TC-024 | Lỗi server khi sửa | Auto (mock) | Pass | |
| GRD-TC-025 | Dialog xóa hiển thị thông tin; Hủy | Auto | Pass | |
| GRD-TC-026 | Xóa thành công | Auto | Pass | |
| GRD-TC-027 | Xóa bản ghi cuối của trang 2 → về trang 1 | Auto (mock) | Pass | |
| GRD-TC-028 | Xóa khối lớp đang được lớp học dùng | Manual | Blocked (thiếu test data) | |
| GRD-TC-029 | Lỗi server khi xóa | Auto (mock) | Pass | |
| GRD-TC-030 → 034 | Phân quyền role khác Admin | — | Out-of-scope | |
| GRD-TC-035 | Locale en | Auto | Pass | |

Tổng automation (25): **21 pass / 4 fail** (đều gắn `@known-bug`). Manual trong phạm vi: 1 blocked (GRD-TC-028).

Ghi chú GRD-TC-013: không có request tạo nào được gửi với giá trị không hợp lệ và giá trị 1, 12 gửi đúng payload (pass); chỉ phần "hiển thị thông báo lỗi của form" fail.

### Lịch sử chạy

| Lần | Thời điểm | Phạm vi | Kết quả | Ghi chú |
| --- | --- | --- | --- | --- |
| Khám phá | 2026-09-16 | Script đọc DOM + response | — | Phát hiện GRD-BUG-001, GRD-BUG-002 |
| Smoke | 2026-09-16 | TC-001, TC-011 | 2 pass | Sau khi chuyển dev server sang port 3000 |
| Full 1 | 2026-09-16 | Toàn bộ | **Hủy** — runner crash sau test 11 | `FATAL ERROR: JavaScript heap out of memory` (máy còn ~0,3 GB RAM trống). Test 8-11 fail trong 9-21 ms trước bước đầu tiên → lỗi môi trường, không tính |
| Nhóm 1 | 2026-09-17 | Danh sách (9) | 7 pass / 2 fail | 4,7 phút |
| Nhóm 4 | 2026-09-17 | Xóa (4) | 4 pass | 55 giây |
| Nhóm 5 | 2026-09-17 | i18n (1) + phân quyền (4, nay out-of-scope) | 5 pass | Chỉ tính GRD-TC-035 |
| Nhóm 2+3 | 2026-09-17 | Thêm mới + chỉnh sửa | GRD-TC-013, 020 fail; còn lại trong phạm vi pass | Chạy lại sau khi sửa spec; phát hiện GRD-BUG-003 qua probe API |

Kết quả trong bảng "Kết quả" lấy từ các lần chạy 2026-09-17. Sau đó chỉ gỡ các test out-of-scope khỏi spec; các test còn lại không đổi nên không chạy lại.

### Thay đổi test code trong lần chạy này

- `tests/e2e/playwright.config.ts`: thêm nhánh dự phòng khi Node không có `process.loadEnvFile` (Node < 20.12). Máy test dùng Node 20.11.0, config cũ lỗi ngay khi nạp. Nhánh cũ giữ nguyên với Node mới.
- `tests/e2e/schools/grade-crud.spec.ts`:
  - GRD-TC-021: dùng Số khối trống thay vì `0` để tách khỏi GRD-BUG-005 (`0` vẫn được assert ở GRD-TC-013). Assertion không nới.
  - GRD-TC-020: thêm assertion status của response `PUT` để có evidence.
  - Fixture dọn dữ liệu: xóa mọi bản ghi `E2E-GRD-` cả trước và sau khi chạy (dọn được cả khi runner crash).
  - Gỡ test out-of-scope: GRD-TC-015, 023 (trùng lặp), GRD-TC-030 → 033 (role khác Admin).
  - `@known-bug`: GRD-TC-003, 007, 013, 020.

### Dữ liệu test

- Tạo trên API develop: chỉ bản ghi tên `E2E-GRD-*` (kể cả probe `E2E-GRD-PROBE-*`), số khối 500-899.
- Sau lần chạy cuối, `GET /v1/grades` còn đúng 7 khối lớp ban đầu (Khối 1-6, Demo1), **0 bản ghi `E2E-GRD-` sót lại**.
- Không xóa/sửa khối lớp thật nào.

## Bug

| Bug | Severity | Status | Tóm tắt |
| --- | --- | --- | --- |
| GRD-BUG-003 | high | open | Admin không sửa được khối lớp: `PUT /v1/grades/{id}` luôn trả 404 "Access denied: Not Found" |
| GRD-BUG-001 | low | open | Cột Ngày tạo hiển thị "01/01/2001" cho dữ liệu có `created_at` mặc định |
| GRD-BUG-002 | low | open | Ô chọn số dòng/trang hiển thị "6" trong khi tải 10 dòng |
| GRD-BUG-005 | low | open | Số khối 0/-1/1.5 hiện tooltip gốc trình duyệt (tiếng Anh) thay vì lỗi của form |
| GRD-BUG-004 | — | closed (không phải bug) | Tạo được khối lớp trùng — chưa có verify trùng lặp theo phạm vi |

## Needs review / Blocked

**BA**

- Requirement `docs/requirements/schools/grade.md` mục 3 vẫn ghi "tránh trùng lặp" và "có trạng thái active/inactive", chưa khớp xác nhận 2026-09-17. Đề nghị cập nhật requirement (kể cả ghi rõ số khối không giới hạn 1-12).
- Hành vi khi xóa grade đang được class sử dụng chưa chốt (đề xuất rule `GRD-BR-NEW-03`) → GRD-TC-028 Blocked.
- Error state của bảng khi tải danh sách lỗi đang hiển thị "Không có dữ liệu"; ngày ở locale `en` vẫn định dạng dd/mm/yyyy — chưa có quy định, không xếp bug.

**BE**

- GRD-BUG-003: xác nhận nguyên nhân 404 "Access denied" của `PUT /v1/grades/{id}`; `docs/security/resources/grades.md` không có action `update`.
- 7 khối lớp có sẵn trên develop có `created_at = 0001-01-01 00:00:00` (dữ liệu cũ).

**Dev FE**

- Nút sửa/xóa trên dòng chỉ có icon, không có accessible name (`aria-label`/`title`) → automation phải chọn theo vị trí nút, dễ vỡ. Chưa xếp bug vì design/requirement chưa quy định.

**Test data / môi trường (đề xuất cập nhật `docs/testing/`)**

- Chưa có cặp khối lớp + lớp học dành riêng cho test → GRD-TC-028 Blocked (không thử trên khối lớp thật vì có thể xóa mất dữ liệu dùng chung).
- `docs/testing/environments.md` ghi `https://localhost:3000`; thực tế dev server chạy `http://localhost:3000` và **API develop chỉ cho phép CORS từ port 3000**.
- `docs/testing/test-automation-workflow.md` ghi "nếu port 3000 bận, Next.js tự chuyển port khác" → không dùng được với API develop; cần giải phóng port 3000.
- Ghi yêu cầu Node (config Playwright gốc cần Node >= 20.12) hoặc giữ nhánh dự phòng đã thêm.
- Máy 8 GB RAM: dev server Turbopack (~1 GB) + Chromium + ứng dụng khác có thể làm runner crash; nên chạy theo nhóm như bên dưới.
- Khi khởi động lại dev server ngay sau khi tắt một instance khác, gặp lỗi Turbopack "Could not find the module ... in the React Client Manifest" (`/vi/login` trả 500); khởi động lại lần nữa thì hết.

## Cách chạy lại

```bash
# Terminal 1: dev server BẮT BUỘC ở port 3000 (CORS của API develop)
pnpm dev            # hoặc: npx next dev --turbopack -p 3000

# Terminal 2: cần TEST_ADMIN_PASSWORD và NEXT_PUBLIC_API_URL trong .env
E2E_BASE_URL=http://localhost:3000 npx playwright test -c tests/e2e/playwright.config.ts tests/e2e/schools/grade-crud.spec.ts

# Chỉ các bug đã biết
E2E_BASE_URL=http://localhost:3000 npx playwright test -c tests/e2e/playwright.config.ts tests/e2e/schools/grade-crud.spec.ts --grep @known-bug

# Máy ít RAM: chạy theo nhóm
for g in "danh sách" "thêm mới" "chỉnh sửa" "xóa" "đa ngôn ngữ"; do
  E2E_BASE_URL=http://localhost:3000 npx playwright test -c tests/e2e/playwright.config.ts tests/e2e/schools/grade-crud.spec.ts --grep "Quản lý khối lớp - $g"
done
```

Nên mở `/vi/login`, `/vi/admin/dashboard`, `/vi/admin/grades` một lần trước khi chạy để dev server compile sẵn.

Sau khi fix GRD-BUG-003: chạy lại nhóm "chỉnh sửa".

## Evidence

- Screenshot, video, trace của test fail: `_bmad-output/test-artifacts/e2e-results/artifacts/` (local, gitignore, không commit).
- Thông báo assertion chi tiết được tóm tắt trong từng bug report.
- Test và script probe chỉ ghi status/message API và dữ liệu khối lớp, không ghi token/password. Trace/video có thể chứa màn hình nhập mật khẩu và header Authorization → không chia sẻ ra ngoài team.
